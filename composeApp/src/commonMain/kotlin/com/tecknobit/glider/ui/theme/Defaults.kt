package com.tecknobit.glider.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.DpSize
import androidx.compose.ui.unit.dp

/**
 * `InputFieldShape` the default shape for the input fields
 */
val InputFieldShape = RoundedCornerShape(
    size = 10.dp
)

/**
 * `ButtonShape` the default shape for the buttons
 */
val ButtonShape = RoundedCornerShape(
    size = 12.dp
)

/**
 * `ButtonSize` the default size for the buttons
 */
val ButtonSize = DpSize(
    width = 300.dp,
    height = 55.dp
)